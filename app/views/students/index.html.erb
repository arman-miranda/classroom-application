<p id="notice"><%= notice %></p>

<h1>Students</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @subjects.each do |subject| %>
      <tr>
        <td><%= subject.name %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to "Logout", destroy_user_session_path, method: :delete %>
