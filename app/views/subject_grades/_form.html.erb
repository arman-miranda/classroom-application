<%= form_for(subject_grade) do |f| %>
  <% if subject_grade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subject_grade.errors.count, "error") %> prohibited this subject_grade from being saved:</h2>

      <ul>
      <% subject_grade.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
